<!DOCTYPE html>
<% include ./header %>
<html>
<title>顧客資料</title>

<link href="/stylesheets/main.css" rel="stylesheet">

<link href="/stylesheets/sidebar.css" rel="stylesheet">

  <body>
    <div id="wrapper">
        <!-- Sidebar -->

          <div id="page-content-wrapper">
              <div class="container-fluid">
                <div class="row">
                    <div class="col-lg-12">
                        <h1 class="page-header">查詢訂單</h1>
                      </div>
                    </div>

                  <!-- <div class="row">
                      <div class="col-lg-12">
                              <form method="post" action="/searchOrder">
                                <p>
                                  月：<input type="text" id="month" name="month" /></input>
                                </p>
                                <p>
                                  日：<input type="text" id="date" name="month" /></input>
                                </p>
                                <a>
                                  <input type="submit" value="查詢"/></input>
                                </a>
                              </form>
                      </div>
                  </div> -->
                </div>
                <table>
                  <tr>
                    <th>ID</th>
                    <th>姓名</th>
                    <th>電子郵箱</th>
                    <th>電話</th>
                    <th>最近消費</th>
                      <th>最近消費頻率</th>
                      <th>最近消費金額</th>
                      <th>RFM編碼</th>
                  </tr>
                  <% if(list){
                    for(var i = 0;i < list.length;i++) { %>
                      <tr>
                        <td><%=list[i].Customer_ID%></td>
                        <td><%=list[i].Name%></td>
                        <td><%=list[i].Email%></td>
                        <td><%=list[i].Phone%></td>
                          <td><%=list[i].recentday%></td>
                          <td><%=list[i].Frequency%></td>
                          <td><%=list[i].Monetary%></td>
                          <td><%=list[i].RFM_Score%></td>
                        <td>

                        </td>
                      </tr>
                      <% }
                    }else{ %>
                      <tr>
                        <td colspan="5" >查無結果</td>
                      </tr>
                      <% } %>
                    </table>
              </div>
          </div>
  </body>
</html>
